# makemap
散歩アプリ（React CRA）ミニプロト。

## 動かし方
1) 解凍してフォルダへ移動
   cd makemap
2) 依存インストール
   npm install
3) （任意）Google Maps API キーを設定
   public/index.html の YOUR_API_KEY を差し替え
   ※ キー未設定でも擬似地図（MapMock）で動作します
4) 起動
   npm start
   → http://localhost:3000
